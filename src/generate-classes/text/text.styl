@import '../../_mixins';


/**
 * Mixin to create classes of text
 */
rs-text($prefix = '', $options = null) {
  // if no $options is passed, process nothing
  if (typeof($options) != 'object' || length($options) < 1) {
    return;
  }

  // generate transition classes from $options
  rs-generate-classes-by-option({
    prefix: ($prefix 'text'),
    property: font,
    data: $options,
  });
}
